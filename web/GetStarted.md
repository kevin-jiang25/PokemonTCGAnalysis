## Get Started

Use Node v25 for this project.

To get started, run the following:

1. Go inside the web directory and run: `npm run server`
2. Go inside the web directory and run : `npm run dev`

Don't forget to run `npm i` to grab the latest imports.

Navigate to localhost:5173.
